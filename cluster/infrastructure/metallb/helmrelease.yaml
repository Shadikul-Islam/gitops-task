apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb-system
spec:
  interval: 5m
  chart:
    spec:
      chart: metallb
      version: "6.4.22"
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  values:
    configInline: {}
