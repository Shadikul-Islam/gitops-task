apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-ingress-nginx
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  ingress-nginx-dashboard.json: |
    {
      "id": null,
      "title": "Ingress-NGINX Logs",
      "panels": [
        {
          "type": "logs",
          "title": "Ingress-NGINX Pod Logs",
          "targets": [
            {
              "expr": "{app=\"ingress-nginx\"}"
            }
          ],
          "gridPos": {"h":13,"w":24,"x":0,"y":0}
        }
      ]
    }
